.$*
.devprj
.idea
.vscode
.contexts
*.db
/automated-release-build/
etc/dev*
etc/*.pem

# dependencies
node_modules/

# artifacts
/dist/
/desktop/dist/
/desktop/gateway-bin/
# the bundled UI: built by `make ui` from frontend/. It was tracked only
# because Go's //go:embed needed it committed; nothing embeds it now.
/desktop/web/

# generated files
.docusaurus
.cache-loader

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
go.work
go.work.sum
zrok-venv

# cache used by local dev cross-build script
/.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.obsidian
CLAUDE.md
AGENTS.md
.codex

.mercurius
mercurius.yaml

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
