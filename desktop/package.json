{
  "name": "tawx-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "TAWX Desktop app",
  "author": "TAW Group",
  "main": "dist/main.js",
  "scripts": {
    "ui": "npm --prefix ../frontend run build",
    "dev": "npm run ui && npm run build && electron .",
    "build": "tsc -p tsconfig.json",
    "dist:mac": "npm run ui && npm run build && electron-builder --mac dir --arm64",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "electron": "^30.5.1",
    "electron-builder": "26.15.3",
    "typescript": "^5.6.3"
  },
  "build": {
    "appId": "com.tawgroup.tawx-desktop",
    "productName": "TAWX Desktop",
    "files": [
      "dist/**/*"
    ],
    "extraResources": [
      {
        "from": "../etc/config.desktop.yaml",
        "to": "etc/config.desktop.yaml"
      },
      {
        "from": "web",
        "to": "web"
      }
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "target": "dir"
    }
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
